{
  "annotations": {
    "list": [
      {
        "builtIn": 1,
        "datasource": "-- Grafana --",
        "enable": true,
        "hide": true,
        "iconColor": "rgba(0, 211, 255, 1)",
        "name": "Annotations & Alerts",
        "type": "dashboard"
      }
    ]
  },
  "description": "CHA (Cluster Health Autopilot) overview — throughput, autonomy, LLM proposer, class policies, reverts, breaker. Driven by cha_* metrics emitted from the aiwatch /metrics endpoint (Phase 2.G).",
  "editable": true,
  "gnetId": null,
  "graphTooltip": 1,
  "iteration": 1717891200000,
  "links": [],
  "panels": [
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "fieldConfig": {"defaults": {"unit": "short"}, "overrides": []},
      "gridPos": {"h": 6, "w": 6, "x": 0, "y": 0},
      "id": 1,
      "title": "Cycle rate (per min)",
      "type": "stat",
      "targets": [
        {"expr": "sum(rate(cha_cycle_total[5m])) * 60", "legendFormat": "cycles/min", "refId": "A"}
      ]
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "fieldConfig": {"defaults": {"unit": "short"}},
      "gridPos": {"h": 6, "w": 6, "x": 6, "y": 0},
      "id": 2,
      "title": "Fresh diagnostics (per min)",
      "type": "timeseries",
      "targets": [
        {"expr": "sum by (severity) (rate(cha_diagnostic_total[5m])) * 60", "legendFormat": "{{severity}}", "refId": "A"}
      ]
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "gridPos": {"h": 6, "w": 6, "x": 12, "y": 0},
      "id": 3,
      "title": "Active class policies",
      "type": "stat",
      "targets": [
        {"expr": "max(cha_policy_active)", "refId": "A"}
      ]
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "gridPos": {"h": 6, "w": 6, "x": 18, "y": 0},
      "id": 4,
      "title": "Breaker open",
      "type": "stat",
      "targets": [
        {"expr": "max(cha_breaker_open)", "refId": "A"}
      ],
      "fieldConfig": {
        "defaults": {
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {"color": "green", "value": null},
              {"color": "red", "value": 1}
            ]
          },
          "mappings": [
            {"options": {"0": {"text": "closed", "color": "green"}}, "type": "value"},
            {"options": {"1": {"text": "OPEN", "color": "red"}}, "type": "value"}
          ]
        }
      }
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 6},
      "id": 5,
      "title": "Autonomy decisions (per min)",
      "type": "timeseries",
      "targets": [
        {"expr": "sum by (result) (rate(cha_autonomy_decision_total[5m])) * 60", "legendFormat": "{{result}}", "refId": "A"}
      ]
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 6},
      "id": 6,
      "title": "LLM proposer outcomes (per min)",
      "type": "timeseries",
      "targets": [
        {"expr": "sum by (outcome) (rate(cha_llm_proposer_total[5m])) * 60", "legendFormat": "{{outcome}}", "refId": "A"}
      ]
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 14},
      "id": 7,
      "title": "Outcomes recorded (per min)",
      "type": "timeseries",
      "targets": [
        {"expr": "sum by (verdict) (rate(cha_outcome_total[5m])) * 60", "legendFormat": "{{verdict}}", "refId": "A"}
      ]
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 14},
      "id": 8,
      "title": "Reverts (per min) — prior fix didn't stick",
      "type": "timeseries",
      "targets": [
        {"expr": "sum by (source) (rate(cha_outcome_revert_total[5m])) * 60", "legendFormat": "{{source}}", "refId": "A"}
      ]
    }
  ],
  "refresh": "30s",
  "schemaVersion": 38,
  "tags": ["cha", "cluster-health-autopilot"],
  "templating": {
    "list": [
      {
        "current": {"selected": false, "text": "Prometheus", "value": "Prometheus"},
        "hide": 0,
        "includeAll": false,
        "label": "Datasource",
        "multi": false,
        "name": "DS_PROMETHEUS",
        "options": [],
        "query": "prometheus",
        "refresh": 1,
        "regex": "",
        "skipUrlSync": false,
        "type": "datasource"
      }
    ]
  },
  "time": {"from": "now-6h", "to": "now"},
  "timepicker": {},
  "timezone": "",
  "title": "CHA — Overview",
  "uid": "cha-overview",
  "version": 1
}
