{{- if (.Values.ai).enabled }}
# AI-companion Deployment (commercial CHA-com tier).
#
# This runs `cha-com watch` — the AI-layered COUNTERPART to the OSS
# watcher, NOT a replacement. The OSS watcher Deployment and the
# diagnose / remediate CronJobs keep running the OSS image and own the
# event-driven probe → fix → Slack / ticketing / DriftReport pipeline.
# This companion polls the SAME merged probe+analyzer catalog on an
# interval and fires the AI tiers (t0 narration → t3 Vault runbooks)
# against NEW diagnostics only, emitting recommendations (and, at t1+,
# click-to-fix URLs signed for the approval-server).
#
# Enabling is purely additive: set ai.enabled=true. No OSS workload is
# swapped or disturbed. See docs/DEPLOYMENT.md.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "cha.fullname" . }}-aiwatch
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cha.labels" . | nindent 4 }}
    cha.bionicaisolutions.com/role: aiwatch
spec:
  # Phase 2.F — when ai.replicas > 1, the watcher uses leader-election
  # so exactly one runs tick() at a time. Default 1 (single-replica
  # noop path; byte-identical to pre-2.F deploys).
  replicas: {{ .Values.ai.replicas | default 1 }}
  selector:
    matchLabels:
      {{- include "cha.selectorLabels" . | nindent 6 }}
      cha.bionicaisolutions.com/role: aiwatch
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        {{- include "cha.selectorLabels" . | nindent 8 }}
        cha.bionicaisolutions.com/role: aiwatch
    spec:
      # Reuses the chart's read-only reader SA — the AI companion only
      # reads the cluster and proposes; it never mutates (every tier is
      # recommendation-only behind human approval).
      serviceAccountName: {{ include "cha.serviceAccountName" . }}
      {{- with .Values.image.pullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.priorityClassName }}
      priorityClassName: {{ . }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: aiwatch
          image: {{ include "cha.aiImage" . }}
          imagePullPolicy: {{ (.Values.ai.image).pullPolicy | default "IfNotPresent" }}
          args:
            {{- include "cha.aiArgs" . | nindent 12 }}
          {{- if (.Values.ai.metrics).addr }}
          ports:
            - name: metrics
              containerPort: {{ (.Values.ai.metrics).port | default 9090 }}
              protocol: TCP
          {{- end }}
          env:
            - name: MY_POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: MY_POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            {{- include "cha.aiEnv" . | nindent 12 }}
            {{- if (.Values.approval).enabled }}
            # t1+ click-to-fix URL signing — same Ed25519 key the
            # approval-server validates with.
            - name: CHA_SIGNING_KEY_PATH
              value: /etc/cha/keys/signing.key
            {{- end }}
          {{- if (.Values.approval).enabled }}
          volumeMounts:
            - name: signing-key
              mountPath: /etc/cha/keys
              readOnly: true
          {{- end }}
          resources:
            {{- toYaml (.Values.ai.resources | default .Values.resources) | nindent 12 }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
      {{- if (.Values.approval).enabled }}
      volumes:
        - name: signing-key
          secret:
            secretName: {{ .Values.approval.signingKey.secretName | default "cha-approval-signing-key" }}
            items:
              - key: signing.key
                path: signing.key
                mode: 0400
            optional: false
      {{- end }}
{{- end }}
