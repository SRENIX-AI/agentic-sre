{{- if .Values.watcher.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "cha.fullname" . }}-watcher
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cha.labels" . | nindent 4 }}
    cha.bionicaisolutions.com/role: watcher
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "cha.selectorLabels" . | nindent 6 }}
      cha.bionicaisolutions.com/role: watcher
  # Recreate avoids two watcher pods both writing DriftReports simultaneously.
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        {{- include "cha.selectorLabels" . | nindent 8 }}
        cha.bionicaisolutions.com/role: watcher
    spec:
      serviceAccountName: {{ include "cha.serviceAccountName" . }}
      {{- with .Values.image.pullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.priorityClassName }}
      priorityClassName: {{ . }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: cha
          image: {{ include "cha.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - watch
            - --live
            - --debounce={{ .Values.watcher.debounce | default "10s" }}
            - --resync-period={{ .Values.watcher.resyncPeriod | default "10m" }}
            - --write-driftreports={{ .Values.driftReport.enabled }}
            {{- if and .Values.slack.alerts.enabled .Values.slack.alerts.secretName }}
            - --slack-alerts=$(SLACK_ALERTS_URL)
            {{- end }}
            {{- if and .Values.slack.critical.enabled .Values.slack.critical.secretName }}
            - --slack-critical=$(SLACK_CRITICAL_URL)
            {{- end }}
            {{- if or (and .Values.slack.alerts.enabled .Values.slack.alerts.secretName) (and .Values.slack.critical.enabled .Values.slack.critical.secretName) }}
            - --slack-post-on-resolved={{ ((.Values.watcher.slack).postOnResolved) | default true }}
            - --slack-repeat-interval={{ ((.Values.watcher.slack).repeatInterval) | default "4h" }}
            {{- end }}
            {{- if and .Values.alertmanager.enabled .Values.alertmanager.url }}
            - --alertmanager-url={{ .Values.alertmanager.url }}
            - --cluster-name={{ .Values.alertmanager.clusterName | default "cluster" }}
            {{- end }}
            {{- if ((.Values.watcher.remedy).enabled) }}
            - --remedy
            {{- if ((.Values.watcher.remedy).dryRun) }}
            - --dry-run
            {{- end }}
            {{- end }}
            {{- if .Values.ticketing.enabled }}
            {{- $pcfg := index .Values.ticketing (.Values.ticketing.provider) }}
            - --ticketing-provider={{ .Values.ticketing.provider }}
            - --ticketing-mcp-url={{ $pcfg.mcpURL }}
            {{- with $pcfg.projectID }}
            - --ticketing-project={{ . }}
            {{- end }}
            {{- with $pcfg.typeID }}
            - --ticketing-type-id={{ . }}
            {{- end }}
            {{- with $pcfg.closedStatusID }}
            - --ticketing-closed-status-id={{ . }}
            {{- end }}
            {{- with $pcfg.severityPriority.critical }}
            - --ticketing-priority-critical={{ . }}
            {{- end }}
            {{- with $pcfg.severityPriority.warning }}
            - --ticketing-priority-warning={{ . }}
            {{- end }}
            {{- with $pcfg.severityPriority.info }}
            - --ticketing-priority-info={{ . }}
            {{- end }}
            {{- with $pcfg.webURLPrefix }}
            - --ticketing-web-url-prefix={{ . }}
            {{- end }}
            {{- range .Values.ticketing.labels }}
            - --ticketing-labels={{ . }}
            {{- end }}
            {{- if .Values.ticketing.dryRun }}
            - --ticketing-dry-run
            {{- end }}
            {{- end }}
          env:
            {{- include "cha.slackAlertsEnv" . | nindent 12 }}
            {{- include "cha.slackCriticalEnv" . | nindent 12 }}
            {{- include "cha.vaultEnv" . | nindent 12 }}
            {{- include "cha.optionalFixerEnv" . | nindent 12 }}
            {{- if and .Values.ticketing.enabled .Values.ticketing.auth.enabled }}
            - name: TICKETING_MCP_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.ticketing.auth.secretName }}
                  key: {{ .Values.ticketing.auth.secretKey }}
            {{- end }}
          resources:
            {{- toYaml .Values.watcher.resources | nindent 12 }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
{{- end }}
